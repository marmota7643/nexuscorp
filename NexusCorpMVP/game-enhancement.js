(() => { 'use strict'; const KEY='nexuscorp_session_v1'; const state={lastSave:0,notifications:[]}; const save=()=>{try{localStorage.setItem(KEY,JSON.stringify({...state,lastSave:Date.now()}));}catch(e){}}; const load=()=>{try{const x=JSON.parse(localStorage.getItem(KEY)||'null');if(x)Object.assign(state,x);}catch(e){}}; const notify=(title,message,type='info')=>{state.notifications.unshift({title,message,type,date:Date.now()});state.notifications=state.notifications.slice(0,20);save();let box=document.getElementById('nexus-notifications');if(!box){box=document.createElement('div');box.id='nexus-notifications';box.setAttribute('aria-live','polite');document.body.appendChild(box);}const el=document.createElement('div');el.className='nexus-toast '+type;el.innerHTML='<strong></strong><span></span>';el.querySelector('strong').textContent=title;el.querySelector('span').textContent=message;box.appendChild(el);requestAnimationFrame(()=>el.classList.add('show'));setTimeout(()=>{el.classList.remove('show');setTimeout(()=>el.remove(),300)},4200);}; const enhance=()=>{load();document.querySelectorAll('button').forEach(b=>{if(!b.dataset.nexusEnhanced){b.dataset.nexusEnhanced='1';b.addEventListener('click',()=>{b.classList.add('nexus-click');setTimeout(()=>b.classList.remove('nexus-click'),180);});}});const money=document.getElementById('ui-money');if(money)money.setAttribute('aria-label','Fondos disponibles');if(!state.lastSave)notify('NexusCorp','Sistema listo. La sesión se guarda localmente.','success');save();}; window.NexusGameEnhancement={notify,save,load,state,version:1}; if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',enhance);else enhance();window.addEventListener('beforeunload',save);})();
