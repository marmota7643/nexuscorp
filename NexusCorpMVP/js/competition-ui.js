// competition-ui.js - Etapa 8: tablero de rivales
const CompetitionUI={init(){if(typeof CompetitionSystem==='undefined')return;this.mount();this.render()},mount(){if(document.getElementById('competition-stage8'))return;const host=document.getElementById('tab-mercado')||document.querySelector('main')||document.body;const box=document.createElement('div');box.id='competition-stage8';box.className='glass-card mt-4';box.innerHTML='<h3>⚔️ Competidores</h3><div id="competition8-list"></div>';host.appendChild(box)},render(){const out=document.getElementById('competition8-list');if(!out)return;out.innerHTML=CompetitionSystem.state.companies.map(c=>`<div class="stat-row"><span>🏢 ${this.esc(c.name)}<small> · ${this.esc(c.strategy||'balanced')}</small></span><span>Cuota ${(c.marketShare*100).toFixed(1)}% · Calidad ${(c.quality*100).toFixed(0)}% · ${c.products.length} productos · ${c.subsidiaries.length} filiales · ${c.alive?'Operando':'En quiebra'}</span></div>`).join('')},esc(v){return String(v??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]))}};