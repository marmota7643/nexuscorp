// bom-ui.js - Etapa 10: interfaz de BOM y costes
const BOMUI={init(){if(typeof BOMSystem==='undefined')return;this.mount();this.render()},mount(){if(document.getElementById('bom-dock'))return;const d=document.createElement('div');d.id='bom-dock';d.innerHTML='<button id="bom-open" class="btn-secondary">🧩 BOM / Costes</button><div id="bom-panel" class="bom-panel hidden"></div>';document.body.appendChild(d);document.getElementById('bom-open').onclick=()=>document.getElementById('bom-panel').classList.toggle('hidden');const s=document.createElement('style');s.textContent='#bom-dock{position:fixed;right:18px;top:100px;z-index:9990}.bom-panel{width:min(720px,calc(100vw - 36px));max-height:70vh;overflow:auto;margin-top:8px;padding:16px;background:rgba(12,16,24,.98);border:1px solid rgba(255,255,255,.12);border-radius:16px;color:#fff}.bom-grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(220px,1fr));gap:10px}.bom-card{padding:12px;border:1px solid rgba(255,255,255,.1);border-radius:12px}.bom-muted{opacity:.7;font-size:.84em}';document.head.appendChild(s)},render(){const p=document.getElementById('bom-panel');if(!p)return;const r=BOMSystem.recipe('smartphone');p.innerHTML='<h2>🧩 BOM industrial</h2><p class="bom-muted">Receta base por smartphone. El coste se calcula desde componentes, no desde una cifra fija.</p><div class="bom-grid">'+Object.entries(r).map(([id,n])=>{const c=BOMSystem.state.components[id];return `<div class="bom-card"><strong>${c?.name||id}</strong><div>${n} × unidad</div><div>$${((c?.unitCost||0)*n).toFixed(2)} por teléfono</div><div class="bom-muted">Lead time: ${c?.leadTime||0} días · Calidad: ${c?.quality||0}</div></div>`}).join('')+'</div><h3>Coste material estimado</h3><strong>$'+BOMSystem.materialCost('smartphone').toFixed(2)+'</strong>'}};
