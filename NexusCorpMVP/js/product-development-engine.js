// product-development-engine.js - ciclo de producto y lanzamiento
const ProductDevelopmentEngine={version:1,state:{products:{},launches:{},versions:{}},init(){this.load()},createProduct(id,name,companyId){this.state.products[id]??={id,name,companyId,status:'concept',quality:.5,cost:0,version:0};this.save();return this.state.products[id]},advance(id,status){const p=this.state.products[id];if(!p)return false;const valid=['concept','prototype','pilot','production','launched','retired'];if(!valid.includes(status))return false;p.status=status;if(status==='launched'){p.version++;this.state.launches[id]={id,productId:id,date:Date.now()};}if(window.NexusIntegration)NexusIntegration.emit('product:status',p);this.save();return p},setEconomics(id,cost,quality){const p=this.state.products[id];if(!p)return false;p.cost=Math.max(0,cost);p.quality=Math.max(0,Math.min(1,quality));this.save();return p},retire(id){return this.advance(id,'retired')},save(){try{localStorage.setItem('nexuscorp_products',JSON.stringify(this.state))}catch(e){}},load(){try{const d=JSON.parse(localStorage.getItem('nexuscorp_products')||'null');if(d)this.state=Object.assign(this.state,d)}catch(e){}}};
