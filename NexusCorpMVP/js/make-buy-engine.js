// make-buy-engine.js - decisión fabricar internamente o comprar a proveedor
const MakeBuyEngine={version:1,state:{decisions:[],benchmarks:{}},init(){this.load()},evaluate(item,{internalCost=0,buyCost=0,capacity=0,required=0,qualityInternal=.8,qualityBuy=.8,leadInternal=0,leadBuy=0,riskInternal=.1,riskBuy=.1}={}){const capacityGap=Math.max(0,required-capacity);const internal=internalCost*(required<=capacity?required:capacity)+capacityGap*buyCost;const buy=required*buyCost;const scoreInternal=internal*(1+qualityInternal<qualityBuy?.03:0)*(1+riskInternal*.2)+leadInternal*.01;const scoreBuy=buy*(1+qualityBuy<qualityInternal?.03:0)*(1+riskBuy*.2)+leadBuy*.01;const choice=scoreInternal<=scoreBuy?'make':'buy';const d={id:'mb_'+Date.now().toString(36),item,choice,internalCost:scoreInternal,buyCost:scoreBuy,capacityGap,date:Date.now()};this.state.decisions.push(d);if(window.NexusIntegration)NexusIntegration.emit('makebuy:decision',d);this.save();return d},save(){try{localStorage.setItem('nexuscorp_makebuy',JSON.stringify(this.state))}catch(e){}},load(){try{const d=JSON.parse(localStorage.getItem('nexuscorp_makebuy')||'null');if(d)this.state=Object.assign(this.state,d)}catch(e){}}};
